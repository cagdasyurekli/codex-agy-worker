{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "kind", "preview_sha256", "payload"],
  "properties": {
    "schema_version": {"type": "integer", "enum": [1]},
    "kind": {"enum": ["agy-model-evidence-campaign-advisory-preview"]},
    "preview_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "payload": {"$ref": "model-evidence-campaign-advisory-summary.schema.json"}
  }
}
