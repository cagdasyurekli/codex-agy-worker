interface:
  display_name: "Verified agy Worker"
  short_description: "Use when Codex should delegate repository work to agy and verify it"
  default_prompt: "Use $agy-worker when this repository task benefits from delegated exploration or implementation. Keep provider transmission approval explicit, inspect the actual diff, run driver-owned checks, and report verified, partially verified, or blocked."
