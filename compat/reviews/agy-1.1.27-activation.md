# AGY 1.1.27 activation evidence

This record supports the v0.18.0 candidate compatibility activation. Publication and final repository verification are separate delivery steps.

The accepted version and account inventory records bind AGY **1.1.27**, reviewed upstream revision `1ae9cb7b51667192c051b73a91099c71e816ca5f`, executable SHA-256 `d583be1344ea9cfa0c45cff2c1342af7837f4833c4edb65e69bee84776a45caa`, and version binding `a1f8b651123f1e95ef7e744ae45e9066967b6ceb0a710ddfd685e54b14b0b0a6`. The inventory has fourteen model slugs, normalized SHA-256 `d5e58ab55e91ebd4a2cd23841c76cbe12b47d607c62cd8c834fc8f6b9f078ad7`. Model mappings are unchanged from the accepted 1.1.26 inventory. Synchronized compatibility artifacts contain the new source, version, and capture bindings.

The official [1.1.27 release](https://github.com/google-antigravity/antigravity-cli/releases/tag/1.1.27) documents a JSON `denied_actions` field for refused headless tool actions and a shutdown fix that flushes trailing conversation history. Version/help inspection found no required changes to the worker's existing command flags. Caller-owned model selection continues to use exact advertised compound slugs without adding a separate effort flag.

The denied-action payload shape remains unverified. A bounded plan-mode canary declined editing before attempting a denied tool call and omitted the field; this is inconclusive for the new upstream field. That canary also read installed AGY built-in guidance outside the staged workspace despite the prompt restriction. It is not clean scoped-read evidence; session mode does not confine host reads. The worker's exact-1.1.27 guard interprets key presence after valid structured-output extraction as `permission_required`, preserves the candidate and actual upstream status, and prevents automatic continuation. Offline fixtures test this controller policy, not an invented upstream payload schema. Successful no-key reports keep their existing behavior.

On 2026-09-06, six explicitly approved starts exercised the pinned executable with `gemini-3.8-flash-high` in normal session mode. Automatic updates were disabled only in provider children. Normal task editing passed direct driver checks. A deliberately incomplete trim-only candidate failed the lowercase check; feedback continued the same conversation, and the repaired trim-and-lowercase candidate passed.

The first repair attempt and the Boost attempt reached provider `SUCCESS`, but their absolute staged paths in `files_changed` failed controller reconciliation. Their worktrees stayed unchanged. The repair retry succeeded; the six-start budget was then closed. Boost identity and file-tool execution were observed, but the full live Boost canary did not pass. No post-fix live Boost success is claimed.

The candidate addresses this concrete report-format mismatch by comparing canonical absolute paths only beneath the exact bound stage as their workspace-relative equivalents. The existing exact mutation set and stage scan remain authoritative; unrelated roots and noncanonical paths are rejected. Offline reconciliation regressions exercise the captured failure shape and boundary negatives. The successful normal/repair calls exercised the unchanged workflow mechanism; the new denial and path-handling changes require separate offline verification and independent acceptance.

Activation promotes 1.1.27 to current, retains 1.1.26 as previous and 1.1.22 as legacy, and keeps 1.1.24, 1.1.16, and 1.1.12 historical. Final stable offline CI and independent release acceptance remain required. Inventory and exercised synthetic paths do not establish provider backend identity, model quality, exhaustive compatibility, native isolation compatibility with a live account, or live optional self-verification. The closed-binary provider backend cannot be independently attested by this evidence.
