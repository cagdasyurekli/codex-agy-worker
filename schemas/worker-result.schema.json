{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "agy worker result envelope",
  "description": "Contract every agy worker job must satisfy. Shape only — Codex verifies truth independently.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status",
    "summary",
    "files_changed",
    "commands_run",
    "tests_run",
    "risks",
    "open_questions",
    "confidence",
    "requires_human"
  ],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["completed", "partial", "blocked", "failed"],
      "description": "Worker's own claim. Never trusted without independent verification."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4000,
      "description": "What was done, in plain prose."
    },
    "files_changed": {
      "type": "array",
      "description": "Every file created/modified/deleted. Checked against git diff by the QA gate.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "change"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "change": { "type": "string", "enum": ["created", "modified", "deleted"] },
          "why": { "type": "string" }
        }
      }
    },
    "commands_run": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Shell commands actually executed, verbatim."
    },
    "tests_run": {
      "type": "array",
      "description": "Worker's test claims. QA gate re-runs these independently.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["command", "passed"],
        "properties": {
          "command": { "type": "string" },
          "passed": { "type": "boolean" },
          "output_excerpt": { "type": "string", "maxLength": 2000 }
        }
      }
    },
    "risks": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Anything the worker believes could break. Empty array is a valid answer, omission is not."
    },
    "open_questions": {
      "type": "array",
      "items": { "type": "string" }
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Self-reported. Treated as a routing hint only, never as evidence."
    },
    "requires_human": {
      "type": "boolean",
      "description": "True if the worker hit anything needing a human decision."
    }
  }
}
